"use client";

// A4: Spec calls for no "How to use this page" collapsible anywhere in
// dashboard. Component preserved as a no-op so existing imports keep working.

export function InlineGuide(_props: { page: string }) {
  return null;
}
